# LearnSQUAR 
Miniproject of reinforcement rearning for a Single Qubit's Unitary Active Reset  
JAX 0.4.35, Python 3.10.11, VSCode 1.95.2, CPU i9-13900H  
  
Goal:  
Train a RL agent to reset a qubit under unitary gate evolution  
  
Definition:  
reset: arbitrary state to |down>:=(1;0)  
qubit: state in Hilbert space PC2, parametrized on a Bloch sphere  
unitary gate evolution: generators are Pauli matrices or 0 times i*δt/2  
episodic RL evironment: tranched time stamp, on which define states, between states define actions and corresponding reward  
RL agent: input current state -> network with inner to-train parameters -> output probality distribution of actions (RL policy)  
  
TODO:  
- Bibliography search on how RL policy normally dependent on parameters, deep neural network? how many layer? additional structure to prevent over-fit?  
- Set up the quantum dynamics of qubit on Bloch sphere --> qubit.py  
- Set up the RL environment that defines the sequences of state space, action space and reward space --> env.py  
 
Questions:  
- ? The expected RL-trained policy should be close to a deterministic one  
- ? Better paramatrization on the Bloch sphere apart from the standard form for this task  
- ? Why maximize the *total* sum of rewards instead of the final one (ok I guess that if we implement the latter it do looks like GRAPE instead of RL, so maybe here we want gradient over every RL's inner parameters that give rise to controlling policy) (and https://www.youtube.com/watch?v=Ds1trXd6pos&list=PLkFD6_40KJIwhWJpGazJ9VSj9CFMkb79A&index=5 might answer this question too)  
- ? policy is probabilistic => can we define implement directly the un-unitary evolution of statistically mixed gate evolution with their corresponding probability generated by RL's inner parameters, so the phase space become the Bloch ball consisted of density matrix  
- ? "educated guess" in Part.3 means the initial values of network's inner parameters  
